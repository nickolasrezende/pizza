const root=document.documentElement;const savedTheme=localStorage.getItem("theme");if(savedTheme)root.setAttribute("data-theme",savedTheme);const themeToggle=document.getElementById("themeToggle");if(themeToggle){themeToggle.textContent=root.getAttribute("data-theme")==="dark"?"☀️":"🌙";themeToggle.addEventListener("click",()=>{const current=root.getAttribute("data-theme");const next=current==="dark"?"light":"dark";root.setAttribute("data-theme",next);localStorage.setItem("theme",next);themeToggle.textContent=next==="dark"?"☀️":"🌙"})}const menuToggle=document.getElementById("menuToggle");const mainNav=document.getElementById("mainNav");if(menuToggle&&mainNav){menuToggle.addEventListener("click",()=>mainNav.classList.toggle("open"))}function getNumber(id){return Number(document.getElementById(id)?.value||0)}function calculateDough(){const flour=getNumber("flour"),hydration=getNumber("hydration"),saltPercent=getNumber("saltPercent"),oilPercent=getNumber("oilPercent"),yeastPercent=getNumber("yeastPercent"),ballWeight=getNumber("ballWeight");const water=flour*hydration/100,salt=flour*saltPercent/100,oil=flour*oilPercent/100,yeast=flour*yeastPercent/100,total=flour+water+salt+oil+yeast,balls=ballWeight>0?Math.floor(total/ballWeight):0;const results=document.getElementById("doughResults");if(!results)return;results.innerHTML=`<div class="results-grid"><div class="result-box"><span>Água</span><strong>${water.toFixed(0)}g</strong></div><div class="result-box"><span>Sal</span><strong>${salt.toFixed(1)}g</strong></div><div class="result-box"><span>Azeite</span><strong>${oil.toFixed(1)}g</strong></div><div class="result-box"><span>Fermento seco</span><strong>${yeast.toFixed(2)}g</strong></div><div class="result-box"><span>Peso total</span><strong>${total.toFixed(0)}g</strong></div><div class="result-box"><span>Bolas aprox.</span><strong>${balls}</strong></div></div>`}let timerInterval=null;let remainingSeconds=30*60;let initialSeconds=remainingSeconds;function updateTimerFromInputs(){const h=getNumber("hours"),m=getNumber("minutes");remainingSeconds=h*3600+m*60;initialSeconds=remainingSeconds;renderTimer()}function renderTimer(){const display=document.getElementById("timerDisplay");if(!display)return;const h=Math.floor(remainingSeconds/3600),m=Math.floor(remainingSeconds%3600/60),s=remainingSeconds%60;display.textContent=`${String(h).padStart(2,"0")}:${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}function startTimer(){if(timerInterval)return;const status=document.getElementById("timerStatus");const label=document.getElementById("timerLabel")?.value||"Etapa";if(remainingSeconds<=0)updateTimerFromInputs();if(status)status.textContent=`${label} em andamento.`;timerInterval=setInterval(()=>{remainingSeconds--;renderTimer();if(remainingSeconds<=0){clearInterval(timerInterval);timerInterval=null;if(status)status.textContent=`${label} finalizada.`;alert(`${label} finalizada! Vai olhar essa massa, cidadão.`)}},1000)}function pauseTimer(){clearInterval(timerInterval);timerInterval=null;const status=document.getElementById("timerStatus");if(status)status.textContent="Timer pausado."}function resetTimer(){clearInterval(timerInterval);timerInterval=null;remainingSeconds=initialSeconds;renderTimer();const status=document.getElementById("timerStatus");if(status)status.textContent="Timer resetado."}["hours","minutes"].forEach(id=>{const el=document.getElementById(id);if(el)el.addEventListener("input",updateTimerFromInputs)});renderTimer();
